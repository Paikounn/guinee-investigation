[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "cd server && npx prisma db push --skip-generate && cd .. && npm run start"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
